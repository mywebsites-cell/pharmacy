const { app } = require("electron"); const Database = require("better-sqlite3"); app.whenReady().then(() => { const db = new Database("C:/Users/ahmad/AppData/Roaming/pharmacypro-desktop/data/pharmacy.db"); try { const row = db.prepare("SELECT * FROM medicines WHERE id = 20").get(); console.log("Inserted Row:", row); } catch (err) { console.error("Error:", err.message); } finally { db.close(); app.quit(); } });
